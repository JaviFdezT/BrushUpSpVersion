<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jft.spverbs.verbos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShowConfig"></activity>
        <activity android:name=".ShowVerbTense" />
        <activity android:name=".ShowVerb" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameOptions" />
        <activity android:name=".ShowStats" />
        <activity android:name=".ShowDict" />
        <activity android:name=".ShowCredits" />
        <activity android:name=".GameCheck" />
    </application>

</manifest>